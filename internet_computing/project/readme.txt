To run from the terminal type:
node server.js

Open a browser and navigate to http://localhost:8080

Once the browser has connected once it will be cached to the local computer and available for offline use.

To close the server: From the terminal server.js is running in ctrl+c. Alternatively from another terminal window type killall node.
